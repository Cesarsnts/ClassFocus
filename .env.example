# Variáveis de ambiente para ClassFocus
# Copie este arquivo para .env e ajuste os valores

# URL de conexão do banco MySQL (recomendado)
# Formato: mysql+pymysql://usuario:senha@host:porta/banco
DATABASE_URL=mysql+pymysql://root:password@localhost:3306/classfocus

# Ou use credenciais separadas (se DATABASE_URL não estiver definida)
DB_USER=root
DB_PASS=password
DB_HOST=localhost
DB_PORT=3306
DB_NAME=classfocus

# Chave secreta da aplicação Flask
SECRET_KEY=sua_chave_super_secreta_aqui
